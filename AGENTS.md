# Repository guidance

Use conventional branch prefixes. Feature branches use `feat/`, bug fixes use
`bug/`, and other branch names should use an equally descriptive non-`codex/`
prefix. Do not create branches with the `codex/` prefix.
